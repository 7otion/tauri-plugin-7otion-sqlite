"$schema" = "schemas/schema.json"

[default]
description = "Allows loading, querying, writing and closing databases."
permissions = [
  "allow-load",
  "allow-close",
  "allow-select",
  "allow-execute",
  "allow-in-transaction",
]
